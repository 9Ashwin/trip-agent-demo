# 家庭旅行规划 Agent —— 一个能联网搜索的网站 Demo

> 这份 Demo 回答一个问题：**「怎么做一个像豆包智能体那样能联网搜索的网站？」**

**仓库地址**：https://github.com/9Ashwin/trip-agent-demo

---

## 30 秒先跑起来（不需要任何 API Key）

```bash
git clone https://github.com/9Ashwin/trip-agent-demo.git
cd trip-agent-demo
./run.sh
```

> 只想看代码不想跑？直接读 `agent.py`（核心 30 行工具调用循环）和
> `static/index.html`（前端）。

然后打开 **http://127.0.0.1:5050**

首次运行会自动建虚拟环境、装依赖。因为没有配置 API Key，它会以**演示模式**启动——
用写死的模拟数据，把完整流程走一遍给你看：填需求 → 滚动显示「正在联网搜索 XXX」→ 输出完整方案。

**先看到效果，再决定要不要接真 key。**

> 想直接上真实联网？**只需要配一个模型 Key** —— 搜索默认走免 Key 的必应抓取，
> 一行搜索配置都不用改。见 [第四节](#四接上真实数据4-步)。

---

## 目录

| 章节 | 内容 |
|---|---|
| [一、先破除一个误解](#一先破除一个误解) | 为什么「改成网页」不能让它联网 |
| [二、正确的架构](#二正确的架构长什么样) | 前端 / 后端 / 外部服务的分工 |
| [三、核心机制](#三最核心的机制工具调用function-calling) | Function Calling 循环 |
| [四、接上真实数据](#四接上真实数据4-步) | 免费模型 + 免 Key 搜索 |
| [五、文件说明](#五文件说明) | 每个文件干什么 |
| [六、用 WorkBuddy 加速](#六想用-workbuddy-加速这套东西) | 怎么下需求给 AI |
| [七、部署上线](#七部署上线) | 发布与 Key 安全 |
| [八、常见坑](#八常见坑) | 排查表 |
| [九、下一步](#九下一步可以做什么) | 扩展方向 |

---

## 一、先破除一个误解

很多人第一反应是：「我把 HTML 改成网页格式，它不就能自己联网了吗？」

**不能。** 这跟文件格式无关，是浏览器的硬性限制：

| 障碍 | 说明 |
|---|---|
| **CORS 跨域** | 浏览器禁止 JS 直接请求携程、12306 等站点 |
| **没有 AI 工具** | 网页里的 JS 没法调用「搜索」这种服务端能力 |
| **API Key 会泄露** | 前端代码用户全都能看到，Key 放前端等于公开 |
| **需要服务端计算** | 搜索 → 解析 → 再喂给模型 → 拼装结果，这串活需要程序在服务器上跑 |

**结论：联网能力必须放在「页面之外」那一层，也就是后端。**

---

## 二、正确的架构长什么样

```
你的浏览器                     你的后端服务器                    外部服务
┌──────────┐   POST 需求   ┌─────────────────┐   工具调用   ┌──────────────┐
│ 前端页面  │ ───────────→ │  Flask 后端     │ ──────────→ │ 必应搜索页    │
│ (HTML)   │               │                 │              │ (免 Key 抓取) │
│          │ ←─────────── │  ① 保管 API Key │              └──────────────┘
│  显示结果 │   SSE 实时推  │  ② 跑 Agent 循环 │   对话请求   ┌──────────────┐
└──────────┘               │  ③ 真的去联网搜  │ ──────────→ │ 大模型 API    │
                           └─────────────────┘              │ (DeepSeek)   │
                                                            └──────────────┘
```

**分工一句话：前端只负责「显示」，后端才负责「干活」。**

---

## 三、最核心的机制：工具调用（Function Calling）

整件事的关键，就在 `agent.py` 里这段循环：

```
1. 后端把 web_search 这个工具「告诉」大模型
      ↓
2. 大模型判断：「我需要知道广州到昆明的机票价」
   → 返回一个 tool_call：{ name: "web_search", query: "广州 昆明 机票 9月30日" }
      ↓
3. 后端收到指令，真的去必应搜一页（免 Key），解析出网页摘要
      ↓
4. 把搜索结果塞回对话历史，再问大模型一次
      ↓
5. 重复 2-4，直到大模型说「信息够了」→ 输出最终方案
```

**重点：大模型自己不能上网。** 是它「指挥」后端去搜的。
豆包智能体、扣子 Bot、以及各种「联网 AI」底层都是这个套路。

代码见：
- `agent.py` → 第 47-141 行，工具定义 + Agent 循环
- `search_tool.py` → 真正发 HTTP 请求去搜索的那 30 行
- `app.py` → Flask 接口，把每一步用 SSE 实时推给前端

---

## 四、接上真实数据（4 步）

> ### 👉 推荐组合（给想直接能用的人）
>
> **搜索默认就是免 Key 的必应抓取 —— 所以你只需要配一个模型 Key。**
>
> | 场景 | 模型 | 搜索 | 成本 |
> |---|---|---|---|
> | **默认（本项目出厂配置）** | **DeepSeek 官方** `deepseek-chat` | **必应抓取（免 Key）** | 模型按量约 ¥0.7/百万 token，几乎可忽略 |
> | **零成本** | 智谱 `glm-4-flash`（永久免费） | **必应抓取（免 Key）** | **0 元** |
> | **要更稳更准**（可选升级） | DeepSeek 官方 `deepseek-chat` | Tavily（免费 1000 次/月） | 同上 |
>
> **DeepSeek 官方的最简配置**（复制进 `.env` 即可，搜索一行都不用改）：
>
> ```
> LLM_API_KEY=sk-你的deepseek密钥
> LLM_BASE_URL=https://api.deepseek.com
> LLM_MODEL=deepseek-chat
> SEARCH_PROVIDER=bing
> ```
>
> 注册地址：https://platform.deepseek.com （新用户送免费额度）
> 优点：**原生支持 function calling**，中文强，OpenAI 接口兼容，零适配成本。

### 第 1 步：拿一个大模型 Key

**想零成本？直接用永久免费的 GLM-4-Flash：**

| 平台 | 免费模型 | 额度 | 说明 |
|---|---|---|---|
| **智谱 AI** ⭐ | `glm-4-flash` | **永久免费** | 中文最强，国内直连，首选 |
| 硅基流动 | Qwen2.5-7B 等 | 9B 以下永久免费 + 新人 2000 万 Token | 一个 Key 通吃几十家模型 |
| 火山引擎（豆包） | Doubao-Lite | 每日 200 万 Token 自动刷新 | 需先在控制台手动开通 |
| 百度千帆 | `ernie-speed-128k` | 永久免费 | 需实名认证 |
| 腾讯混元 | `hunyuan-lite` | 永久免费 | 生态内方便 |

**智谱的填法**（复制进 `.env`）：

```
LLM_API_KEY=你的智谱key
LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
LLM_MODEL=glm-4-flash
```

> 去 https://open.bigmodel.cn 注册 → 「API Keys」生成。

**想效果更好、愿意花点小钱？用 DeepSeek**（中文强，¥0.7/百万 token，最低一档）：

```
LLM_API_KEY=sk-你的key
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat
```

**⚠️ 选模型时必须确认一件事：支持 function calling（工具调用）。**
本项目靠它让模型「指挥后端去搜索」，不支持的话模型永远不会调用 `web_search`。
GLM-4-Flash、DeepSeek、GPT-4o-mini、Qwen 系列都支持；一些小模型或老模型可能不支持。

> 其余平台只要兼容 OpenAI 接口格式，改 `LLM_BASE_URL` 和 `LLM_MODEL` 就行。

#### 方案 D：OpenCode Go 套餐（$10/月，开源编码模型）

[OpenCode Go](https://opencode.ai/docs/go/) 是订阅制的模型网关，走 `/zen/go/v1`。
它有两个**额外要求**（不加会被拒），本项目已用环境变量支持：

```
LLM_API_KEY=你的opencode key
LLM_BASE_URL=https://opencode.ai/zen/go/v1
LLM_MODEL=deepseek-v4.1-flash
LLM_USER_AGENT=trip-agent-demo/1.0      # 必须标识自己的客户端，不能用通用 SDK 名
LLM_SESSION_HEADER=x-opencode-session   # 每段会话要发稳定 session id
```

**两个容易踩的坑：**
1. **模型 ID 必须是带小数点的** —— `deepseek-v4.1-flash`（DeepSeek V4.1 Flash）和
   `deepseek-v4-flash`（DeepSeek V4 Flash）是**两个不同的模型**，写错了会 400/404
2. **缺 `x-opencode-session` 会直接 400**：`MissingSessionID ... cannot be routed efficiently`。
   `agent.py` 里每次请求会生成一个 uuid 作为会话 id

> ⚠️ OpenCode Go 官方说明它「面向 OpenCode 等编码智能体，会监测异常流量」。
> 拿它跑通用网站属于超出设计用途，**自行评估风险**，别做公开服务。
> 当前可用模型列表：`curl https://opencode.ai/zen/go/v1/models`

### 第 2 步：搜索 —— 默认免 Key，开箱即用

#### 🟢 默认方案：免 Key 必应抓取（零配置）

**本项目的出厂配置就是免 Key 的**，`.env.example` 里已经写好了：

```
SEARCH_PROVIDER=bing
TAVILY_API_KEY=
```

**你什么都不用改** —— 只要能联网就有真实搜索结果。本项目内置了「抓取必应搜索结果页」的实现，**一个搜索 Key 都不用配**。

实测可用：单次查询稳定返回 5 条真实结果（标题 + 链接 + 摘要）。
用这种方式跑完整流程，能拿到 **24 次搜索 / 120 条结果 / 60 个来源**，方案质量可用。

**代价要清楚：**

| 方面 | 说明 |
|---|---|
| 稳定性 | 属于网页抓取，**必应改版就可能失效**，没有任何 SLA |
| 限流 | 高频调用会被限流或弹验证码 |
| 条款 | 不符合对方使用条款，**别用于正式产品** |
| 精准度 | 不如 Tavily，查「机票价格」这类交易型需求时结果偏泛 |

> **结论**：学习、Demo、个人低频使用完全够用；要做正式产品再升级到下面的选择。

#### 🟡 可选升级：Tavily / 博查（要更稳更准时用）

| 提供方 | 特点 | 地址 |
|---|---|---|
| **Tavily** | 专为 AI Agent 设计，免费 1000 次/月，国际线路 | https://tavily.com |
| **博查** | 国内可直连，按次计费，国内网络首选 | https://open.bochaai.com |

```
SEARCH_PROVIDER=tavily
TAVILY_API_KEY=tvly-你的key
```

> 注意：`SEARCH_PROVIDER` 一旦显式写成 `tavily`，就必须有 Key；
> 想「有 Key 就用、没 Key 自动回落必应」，填 `auto`。

#### ❌ 已废弃：DuckDuckGo（实测不可用，别再试了）

代码里保留了 DDG 兜底，但实测有**双重问题**：

1. **网络不通** —— `duckduckgo.com` 与 `api.duckduckgo.com` 在国内无法访问，实测连接直接失败（`curl` 得到 `000`）。注意不是沙箱问题：同一环境下 `tavily.com` 返回 302、`baidu.com` 返回 200，说明是被针对性阻断。
2. **接口性质不对** —— 免 Key 只能用 Instant Answer API，它是「百科摘要」接口（返回词条 Abstract 和 RelatedTopics），**不是搜索引擎**。对「广州 昆明 机票 9月30日 价格」这类查询通常返回空。

> **为什么「用 langchain 之类的库」解决不了？**
> LangChain 本身不是搜索引擎，它只是**包装器**。它提供的搜索工具底层要么需要 API Key
> （Tavily / Serper / Bing 官方 API），要么就是同一个 DDG 接口
> （`DuckDuckGoSearchRun`）。真正免 Key 且国内可用的只有：抓取搜索结果页（本项目的必应方案）
> 或自建 SearXNG 实例。

#### 关于百度 / 维基百科

实测均不可用：百度搜索页返回反爬页面（HTTP 200 但只有 1.4KB 空壳），维基百科被网络限制。
所以**免 Key 方案里，必应是目前唯一可用的**。

### 第 3 步：重启

```bash
./run.sh
```

启动横幅会从「演示模式」变成「真实模式」，页面上方的徽章也会变绿。

### 第 4 步：填需求，点按钮

看着它一步步联网搜索，然后吐出真实方案。

---

## 五、文件说明

| 文件 | 干什么的 |
|---|---|
| `app.py` | Flask 服务：托管页面 + `/api/plan` 接口 + SSE 实时推送 |
| `agent.py` | **核心**：Agent 循环，让大模型自己决定何时联网 |
| `search_tool.py` | 搜索工具层，默认免 Key 必应，五路自动降级 |
| `mock_agent.py` | 演示模式，无需 Key 也能跑通全流程 |
| `static/index.html` | 前端工作台（单文件，无框架依赖） |
| `.env.example` | 配置模板，复制成 `.env` 后填 |
| `run.sh` | 一键启动 |

---

## 六、想用 WorkBuddy 加速这套东西

这份 Demo 本身就可以用 WorkBuddy 生成 —— 你只要说清楚**你要的结果**，不用指定实现方式：

```
帮我做一个家庭旅行规划网站 Demo：
- 前端一个表单，填 出发地/目的地/日期/人数/预算/偏好
- 点提交后，后端调用大模型，并且模型能自己联网搜索
- 搜索结果要实时显示在页面上（用 SSE 流式推）
- 后端用 Python Flask，API Key 只放后端
- 样式：墨绿 + 米白极简风
- 没配 API Key 时要能进演示模式，用假数据跑通全流程
- 给我 README，一步步教我朋友怎么跑起来
```

**反面例子**（这样问会绕远路）：

```
帮我接入大模型 / 帮我链接扣子 / 把这个 HTML 改成能联网的
```

因为「接入」「链接」是**手段**，而且往往猜错了方向。说清楚**输入什么、中间发生什么、输出什么**，AI 才能选对方案。

---

## 七、部署上线

### 方式一：WorkBuddy 的「发布为应用」（本项目已用这个方式发布）

在 WorkBuddy 里对它说一句「发布这个项目」即可，不用自己买服务器、配域名。
发布前确认两件事：

1. 服务绑定 `0.0.0.0`（本项目 `app.py` 已改好）——绑定 `127.0.0.1` 的话线上访问不到
2. 读取 `PORT` 环境变量（已改好）——部署平台会注入这个变量

### 方式二：自己部署

- **后端**：Railway / Render / 腾讯云 Serverless（都支持 Python + 环境变量）
- **前端**：如果只用静态版，可以单独丢到 Vercel / Netlify
- **环境变量**：Key 配在平台的环境变量面板里，**不要**写进代码或提交到 Git

### ⚠️ 线上版本：搜索无需 Key，但模型仍需 Key

**搜索默认免 Key（必应抓取），所以线上唯一必须配的是模型 Key。**

线上沙箱里没有你的 `.env`。想让**在线版**也能真实联网搜索：

1. 在本地把 `LLM_API_KEY` 填进 `.env`（搜索保持 `SEARCH_PROVIDER=bing` 即可）
2. 重新发布一次（覆盖线上版本）

> 也就是说：**GitHub 上的代码永远不需要带任何 Key**（模型 Key 只放本地/部署环境），
> 而**部署环境可以带 Key**（那才是真正要调用模型的地方）。两边搜索行为完全一致 —— 都是免 Key 必应。

> **安全提醒（重要）**
> 线上版本一旦配了 Key，就等于把「调用额度」公开了 —— 任何拿到分享链接的人
> 都能消耗你的额度。给朋友看 Demo 用**演示模式**就够了。
> 如果你确实要上线带 Key 的版本，本项目已内置 **按 IP 限流**兜底：
>
> ```
> RATE_LIMIT_PER_HOUR=20     # 每个 IP 每小时最多 20 次，0 = 关闭
> ```
>
> 超过会返回 `429`，前端会提示「请求太频繁了」。这个配额对朋友试用绰绰有余，
> 但能挡住恶意刷量。注意限流是**进程内存**实现，多实例部署需换成 Redis。
>
> 发布前记得确认 `.env` 没有被一起上传（本项目 `.gitignore` 已排除，但仍建议
> 手动核对上传清单）。

---

## 八、常见坑

| 现象 | 原因 | 解决 |
|---|---|---|
| POST 请求返回 **403** | macOS 的 5000 端口被 AirPlay 接收器占用 | 用 5050 端口（本项目已默认） |
| 页面上方一直是「后端未连接」 | 后端没启动 | 先跑 `./run.sh` |
| 一直返回「演示数据」 | 没配 `LLM_API_KEY` | 填 `.env` 后重启 |
| 搜索结果很少 | 用了 DuckDuckGo（国内不通 / 是百科接口） | 配 Tavily 或博查，见第四节 |
| 模型不调用搜索 | 模型不支持 function calling | 换 deepseek-chat / gpt-4o-mini 等 |
| 页面提示「请求太频繁」**429** | 触发了按 IP 限流 | 等一小时，或调大 `RATE_LIMIT_PER_HOUR` |

### Q：能不能直接复用 WorkBuddy 的模型能力，不自己的 API Key？

**不能。** 原因：

1. WorkBuddy 是 **客户端 AI 办公产品**，不是模型 API 服务商 —— 官方文档里只列了文档生成、
   数据分析、PPT 生成等能力模块，**没有对外的开放 API / API Key**。
2. 它按**积分套餐**计费（体验版每月 500 积分、标准版 2000 积分……），
   没有提供「按调用量付费给第三方网站用」的接口形态。
3. 架构上它处在**终端**位置：你在 WorkBuddy 里对话，它调用模型、操作你本地文件。
   它不是一个能被别的网站反向调用的中间层。

**那「复用 WorkBuddy 能力」的正确姿势是什么？**

不是在运行时调它，而是在**开发时**用它：

| 你想做的事 | 正确的做法 |
|---|---|
| 生成/修改这个项目的代码 | 直接对 WorkBuddy 说需求，它写代码、跑测试、发布上线 |
| 每天自动跑一次旅行规划 | 用 WorkBuddy 的**定时任务**能力，不用部署网站 |
| 连自己的数据源（邮箱、表格、数据库） | 用 WorkBuddy 的 **MCP 连接器** |
| 做一个给别人用的联网网站 | ← 就是本项目，**必须有自己的模型 Key** |

**一句话：WorkBuddy 是帮你「造工具」的，不是给别人「当零件」的。**

> 顺带提醒：本项目已发布的在线版本用的是**演示模式**（假数据）。
> 想让在线版真实联网，需要在本地 `.env` 填 Key 后重新发布 ——
> 但那等于把调用额度公开了，只给朋友看 Demo 的话没必要。

---

## 九、下一步可以做什么

- **加流式输出**：让方案文字也一个字一个字蹦出来（对 `LLM` 请求开 `stream=True`）
- **多轮对话**：把 `messages` 存在浏览器 localStorage，支持追问「换一家酒店」
- **导出 PDF**：前端加 `window.print()` + 打印样式
- **部署上线**：后端丢到 Railway / 腾讯云 Serverless，前端丢到 Vercel
- **换成别的 Agent**：改 `agent.py` 里的 `SYSTEM_PROMPT` 和工具定义，就是另一个领域的 Agent 了

---

**一句话总结：网页负责好看，后端负责干活，大模型负责指挥。** 三者分工搞清楚了，这类「能联网的 AI 网站」就没什么神秘的了。
